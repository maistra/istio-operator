# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/extensions.istio.io_wasmplugins.yaml
- bases/networking.istio.io_destinationrules.yaml
- bases/networking.istio.io_envoyfilters.yaml
- bases/networking.istio.io_gateways.yaml
- bases/networking.istio.io_proxyconfigs.yaml
- bases/networking.istio.io_serviceentries.yaml
- bases/networking.istio.io_sidecars.yaml
- bases/networking.istio.io_virtualservices.yaml
- bases/networking.istio.io_workloadentries.yaml
- bases/networking.istio.io_workloadgroups.yaml
- bases/operator.istio.io_istiorevisions.yaml
- bases/operator.istio.io_istios.yaml
- bases/security.istio.io_authorizationpolicies.yaml
- bases/security.istio.io_peerauthentications.yaml
- bases/security.istio.io_requestauthentications.yaml
- bases/telemetry.istio.io_telemetries.yaml
#+kubebuilder:scaffold:crdkustomizeresource

patchesStrategicMerge:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
#- patches/webhook_in_istios.yaml
#+kubebuilder:scaffold:crdkustomizewebhookpatch

# [CERTMANAGER] To enable cert-manager, uncomment all the sections with [CERTMANAGER] prefix.
# patches here are for enabling the CA injection for each CRD
#- patches/cainjection_in_istios.yaml
#+kubebuilder:scaffold:crdkustomizecainjectionpatch

# the following config is for teaching kustomize how to do kustomization for CRDs.
configurations:
- kustomizeconfig.yaml
